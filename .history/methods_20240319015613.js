/*
beforeAll(fn,timeout)
beforeEach (fn,timeout)
*/
beforeAll(() => {
  //connect to database
  // empty testing table
  // add dummy data for testing
  // prepare everything before testing
  //default timeout is 5s
  // beforeAll is running before all tests
});
